namespace Lexer
{
    public struct MemoryInfo
    {
        // Represents a name/value pair stored in memory. Used when
        // serializing or debugging variable information.
        public string name;
        public decimal value;
    }
}
